[project]
name = "capacity-estimation"
version = "0.1.0"
description = "Team capacity estimation app (NiceGUI + in-memory SQLite)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "nicegui>=3.16.0",
    "sqlalchemy>=2.0.52",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
